# AWS Configuration
# Note: For AWS Amplify deployment, use DYNAMODB_REGION instead of AWS_REGION
DYNAMODB_REGION=us-east-1
COMMON_TABLE_NAME=Common

# For local development with DynamoDB Local, uncomment:
# DYNAMODB_ENDPOINT=http://localhost:8000

# App Configuration
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Support/Donation (optional)
NEXT_PUBLIC_VENMO_URL=https://venmo.com/u/yourhandle
NEXT_PUBLIC_VENMO_HANDLE=@yourhandle

# Instructions:
# 1. Copy this file to .env.local
# 2. Update the values with your actual configuration
# 3. For local dev, use DynamoDB Local (see README)
# 4. Never commit .env.local to version control
